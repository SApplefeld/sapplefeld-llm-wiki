# Local Claude Code permission overrides. Personal to this machine.
.claude/settings.local.json

# Windows and editor cruft.
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# inbox/ is deliberately NOT ignored: an unprocessed drop must stay visible
# in `git status` until the ingest skill moves it into sources/.
